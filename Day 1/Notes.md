## Notes for Day 1 of learning pyhton ##

## 1- What I have learned :
**input()**
- Is a function that asks the user to type something and returns what they typed.
- You can ask for the name, age, address and eny information can be stored in python.
Example : 
    name = input("What's your name? ")
    print("Hello, " + name) 
    Python stores "Abdulrhman" in the variable "name".


**print("...")**
- It's a function can display something on the screen, that I'm writing between "...".
Example :
    print("Hello world, ")
    Pyhton will display what you write between "...".


**Bug**
- A bug is a mistake in code that causes incorrect or unexpected behavior.


**function**
- A function is a set of instructions that runs when you call it.


**Variable**
- Variables can store a value (Numbers, Name, Info...) in the program.
    name = input("What's your name ? ") **The variable here is name**


**Separator**
- sep (separator): Specifies what is printed between multiple objects in print(). It only works when the objects are separated with commas ,, not when they are joined with +.
Example :
    print(name, age, sep='-') **this is working**
    print(name + age + sep='-') **doesn't working**


**Remove the whitspace in output**
Example :
    name = name.strip()


**Capitalize the output**
Example : 
    name = name.capitalize()


**Capitalize the output (The first word and second)**
    name = name.title()


**Calculator**
- I use int to convert the str into integer.
Example :
    x = int(input("x : "))
    y = int(input("y : "))


**Float**
- It's uses to calculat the decimal numbers (Ex : 6.67).
Example :
    x = float(input("x : "))

**string**
- It's any thing type in ("")
Example: 
    print("Any thing type here is a string")


**Integer**
- It's any numbers type in python.
Example:
    print(12 any thing type without "" is integer)


## 2- The chalnges I face :
- I have not face any big challenge, but I have just to focus.

## 3- The mistakes I make :
- I have do not mistake.

## 4- Time Spent :
- Learning: 1.45 h
- Practice: 2 h

## 5- Helpful Resources :
https://youtu.be/JP7ITIXGpHk?si=vNjmsB61q-ZEBnTw

## 6- Summarize :
- In the practice.py I'm practicing how to store the variable and print it.
- In the practice1.py I'm practicing how to print any thing between objects using sep="" function.
- In the practice2.py I'm practicing how to print the end of objects using end="" function.
- In the practice3.py I'm practicing how to print the output capitalize.
- In the pracitce4.py I'm practicing how to make a calculator using int.
- In the pracitce5.py I'm practicing how to calculate two decimal numbers using float function.